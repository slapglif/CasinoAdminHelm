apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "AdminHelm.fullname" . }}-ingress
  labels:
    app.kubernetes.io/name: {{ include "AdminHelp.name" . }}
    helm.sh/chart: {{ include "AdminHelm.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "AdminHelm.fullname" . }}-service
                port:
                  name: http
